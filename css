.dropdown-container {
  position: relative; /* Ensures dropdown-options are positioned relative to this container */
  z-index: 10; /* Make sure it's above other elements */
}

.dropdown-options {
  position: absolute; /* Allow it to be placed outside of normal document flow */
  top: 100%; /* Position it directly below the dropdown-selected */
  left: 0;
  right: 0;
  z-index: 1000; /* High enough to be above other content */
  background: white;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  max-height: 300px; /* Adjust for scrolling long lists */
  overflow-y: auto; /* Add scroll behavior */
  display: block; /* Ensure it's displayed */
  visibility: visible !important; /* Override any hidden styles */
  opacity: 1 !important; /* Fully visible */
}

body {
  overflow: visible; /* Ensure no clipping occurs */
}
